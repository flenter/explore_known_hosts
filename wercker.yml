box: wercker/ubuntu12.04-webessentials
build:
    steps:
        - script:
            name: show known_hosts
            code: |
                export
                ls -la $HOME
                ls -la $HOME/.ssh
                ssh ssh.github.com
